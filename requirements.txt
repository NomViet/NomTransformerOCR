# Core (Python 3.10+ / 3.12 compatible)
numpy>=1.26.0
einops>=0.6.0
editdistance>=0.6.0
opencv-python>=4.8.0
matplotlib>=3.7.0
Pillow>=9.0.0
tqdm>=4.65.0
requests>=2.28.0

# PyTorch / Lightning (install separately: pip install torch torchvision pytorch-lightning)
# torch
# torchvision
# pytorch-lightning

# CLI / config
jsonargparse[signatures]>=4.27.0
typer>=0.9.0

# Optional: parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Optional: TensorFlow (for evaluate.py metrics)
# tensorflow>=2.15.0

# Dev (optional)
# flake8>=6.0.0
# black>=23.0.0
# isort>=5.12.0
# jupyter>=1.0.0
